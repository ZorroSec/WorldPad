<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>WorldPad</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
            background-color: #f3f3f3;
        }
        header.header{
            background: #fff;
            color: black;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px;
            box-shadow: 0 0 2.2em rgba(0,0,0,.3);
            font-weight: bold;
        }
        section.conteiner div.posts div.post{
            margin: auto;
            padding: 1em;
            color: black;
            box-shadow: 1px 0 3px rgba(0,0,0,.3);
            background: #ffffff;
            margin-top: 1em;
            margin-bottom: 1em;
        }
        section.conteiner div.addNewPost{
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: #ffffff;
            box-shadow: 1px 0 3px rgba(0,0,0,.3);
            padding: 1em;
        }
        section.conteiner div.addNewPost div.headerAddNewPost{
            text-align: center;
        }
        section.conteiner div.addNewPost form div.newPost div.infoOptionsPost{
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        section.conteiner div.addNewPost form div.newPost div.infoOptionsPost div.date input{
            padding: 10px 20px;
            width: 100%;
            border: none;
            outline: none;
            background-color: none;
            border-bottom: 1px solid black;
            color: black;

        }
        section.conteiner div.addNewPost form div.newPost div.infoOptionsPost div.submit input{
            border: none;
            outline: none;
            background-color: #23abff;
            color: white;
            border-radius: 5px;
            padding: 10px 20px;
        }
        section.conteiner div.addNewPost form div.newPost div.postContent textarea{
            padding: 10px;
            color: black;
            outline: none;
            width: 100%;
            resize: none;
        }
        section.conteiner div.addNewPost form div.newPost div.titlePost div.title input{
            border: none;
            outline: none;
            background: none;
            color: black;
            border-bottom: 1px solid black;
            width: 100%;
            padding: 10px 20px;
            margin-bottom: 5px;
        }
        section.coteiner div.addNewPost form div.newPost div.uploadImage{
            display: flex;
            align-items: center;
            justify-content: center;
        }
        div.img input{
            display: none;
        }
        div.img label{
            border: none;
            outline: none;
            background-color: #23abff;
            color: white;
            border-radius: 5px;
            width: 100%;
            padding: 10px 20px;
            text-transform: uppercase;
            text-align: center;
        }
    </style>
    <title>Document</title>
</head>
<body>
    <header class="header">
        <div class="logo">
            <h1>
                WorldPad
            </h1>
        </div>
    </header>
    <section class="conteiner">
        <div class="addNewPost">
            <div class="headerAddNewPost">
                <h2>
                    Add New Post
                </h2>
            </div>
            <form action="/add" method="POST">
                <div class="newPost">
                    <div class="infoPost">
                        <div class="infoOptionsPost">
                            <div class="date">
                                <input type="datetime-local" name="data" id="data">
                            </div>
                            <div class="submit">
                                <input type="submit" value="Enviar" name="submit" onclick="{{ submitBtn }}">
                            </div>
                        </div>
                        <div class="postContent">
                            <textarea name="post" id="post" cols="30" rows="10"></textarea>
                        </div>
                        <div class="titlePost">
                            <div class="title">
                                <input type="text" name="title" id="title" placeholder="Title">
                            </div>
                        </div>
                        {{!-- <div class="uploadImage">
                            <div class="img">
                                <label for="file">Carregar Imgem</label>
                                <input type="file" name="file" id="file">
                            </div>
                        </div> --}}
                    </div>
                </div>
            </form>
        </div>
    </section>
</body>
</html>