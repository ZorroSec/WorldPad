<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>WorldPad</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
            background-color: #f3f3f3;
        }
        header.header{
            background: #fff;
            color: black;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px;
            box-shadow: 0 0 2.2em rgba(0,0,0,.3);
            font-weight: bold;
        }
        section.conteiner div.posts div.post{
            margin: auto;
            padding: 1em;
            color: black;
            box-shadow: 1px 0 3px rgba(0,0,0,.3);
            background: #ffffff;
            margin-top: 1em;
            margin-bottom: 1em;
        }
        section.conteiner div.posts div.post div.infoUser{
            display: flex;
            margin-bottom: 15px;
        }
        section.conteiner div.posts div.post div.infoUser div.userImg{
            width: 30px;
            height: 30px;
            border-radius: 50px;
            background: #333;
            margin-right: 1em;
        }

    </style>
    <title>Document</title>
</head>
<body>
    <header class="header">
        <div class="logo">
            <h1>
                WorldPad
            </h1>
        </div>
    </header>
    <section class="conteiner">
        <div class="addNewPost">
            <div class="newPost">
                <div class="infoPost">
                    <div class="infoOptionsPost">
                        <div class="date">
                            <input type="datetime-local" name="data" id="data">
                        </div>
                    </div>
                    <div class="postContent">
                        <textarea name="post" id="post" cols="30" rows="10"></textarea>
                    </div>
                    <div class="username">
                        <div class="name">
                            <input type="text" name="nome" id="nome" placeholder="nome">
                        </div>
                    </div>
                    <div class="uploadImage">
                        <div class="img">
                            <label for="file">Carregar Imgem</label>
                            <input type="file" name="file" id="file">
                        </div>
                    </div>
                    <div class="submit">
                        <input type="button" value="Enviar" name="sumbit" onclick="{{ submitBtn() }}">
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>